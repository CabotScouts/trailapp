REDIS_HOST="${DOCKER_NAMESPACE}_redis"
REDIS_PASSWORD=null
REDIS_PORT=6379

PUSHER_HOST="${DOCKER_NAMESPACE}_websockets"
PUSHER_PORT="${APP_WEBSOCKET_PORT}"
PUSHER_APP_ID="${DOCKER_TAG}"

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_HOST="${APP_WEBSOCKET_HOST}"
MIX_PUSHER_PORT="${APP_WEBSOCKET_PORT}"
